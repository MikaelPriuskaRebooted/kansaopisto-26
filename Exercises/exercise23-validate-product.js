const product = {
  id: "P001",
  name: "Coffee",
  category: "Beverages",
  price: 8.5,
  stock: 12,
};

// Return true only if:
// - product is an object (null should not count as a valid object)
// - id, name, category are strings
// - price is a number
// - stock is a number
function validateProduct(product) {

}

console.log(validateProduct(product));

// Expected output:
// true
