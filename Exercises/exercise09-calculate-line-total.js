const item = { productId: "P001", qty: 3 };
const product = { id: "P001", name: "Coffee", price: 8.5 };

// Return the total money for this one order item.
function calculateLineTotal(item, product) {

}

console.log(calculateLineTotal(item, product));

// Expected output:
// 25.5
